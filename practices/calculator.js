const addition = (x, y) =>{
    const result = x + y;
    return result;
}

// export default function subtract(x, y){
//     const result = x - y;
//     return result;
// }

export default function subtract(x, y) {
    const result = x - y;
    return result;
}

// module.exports = { addition, subtract};